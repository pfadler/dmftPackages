diff --git a/deps/CMakeLists.txt b/deps/CMakeLists.txt
index 66a306ba..5b59f4d7 100644
--- a/deps/CMakeLists.txt
+++ b/deps/CMakeLists.txt
@@ -52,8 +52,6 @@ if(PythonSupport)
   external_dependency(Cpp2Py
     GIT_REPO https://github.com/TRIQS/cpp2py
     VERSION 2.0
-    GIT_TAG unstable
-    BUILD_ALWAYS
   )
 endif()
 
@@ -61,7 +59,6 @@ endif()
 external_dependency(GTest
   GIT_REPO https://github.com/google/googletest
   GIT_TAG main
-  BUILD_ALWAYS
   EXCLUDE_FROM_ALL
 )
 
@@ -71,7 +68,6 @@ if(NOT IS_SUBPROJECT AND Build_Benchs AND NOT ASAN AND NOT UBSAN)
   external_dependency(google-bench
     GIT_REPO https://github.com/google/benchmark
     GIT_TAG main
-    BUILD_ALWAYS
     EXCLUDE_FROM_ALL
   )
 endif()
diff --git a/test/c++/CMakeLists.txt b/test/c++/CMakeLists.txt
index b27ebe94..d3c10e74 100644
--- a/test/c++/CMakeLists.txt
+++ b/test/c++/CMakeLists.txt
@@ -17,7 +17,7 @@ foreach(test ${all_tests})
   #MESSAGE("${test_name} with option ${ARGV1}")
   get_filename_component(test_dir ${test} DIRECTORY)
   add_executable(${test_name} ${test})
-  target_link_libraries(${test_name} ${PROJECT_NAME}_c gtest_main ${PROJECT_NAME}_warnings h5::hdf5)
+  target_link_libraries(${test_name} ${PROJECT_NAME}_c gtest gtest_main ${PROJECT_NAME}_warnings h5::hdf5)
   target_compile_options(${test_name}  PRIVATE "${ARGV1}")
   set_property(TARGET ${test_name} PROPERTY RUNTIME_OUTPUT_DIRECTORY ${CMAKE_CURRENT_BINARY_DIR}/${test_dir})
   add_test(NAME ${test_name} COMMAND ${test_name} WORKING_DIRECTORY ${CMAKE_CURRENT_BINARY_DIR}/${test_dir})
